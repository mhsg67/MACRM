package MACRM.General

import org.joda.time.DateTime

/*
 * TODO: it should be extended to represent utilization of both CPU and memory
 */
class BasicUtilization(var percent: Double = -1.0)

class Container(var resource: BasicResource, var id: Int, var userId: Int, var jobId: Int, var utilization: BasicUtilization = null) 
